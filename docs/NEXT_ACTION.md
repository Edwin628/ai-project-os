# Next Action

Keep this file limited to the current 1-3 actionable next steps.

## Current Next Steps

1. 检查本次生成的初始化器文件，并提交变更。
2. 提交完成后创建并推送 `v0.1.0` Git tag。
3. 在干净项目目录中测试公开安装 URL。

## Blockers

- 版本化安装 URL 依赖 GitHub 上存在 `v0.1.0` tag。

## Notes

- 默认安装路径保持版本化；`main` 只用于开发测试。
