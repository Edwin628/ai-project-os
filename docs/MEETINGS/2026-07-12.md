# 2026-07-12

## Summary

- 回答用户关于「已有 CLAUDE.md 的项目跑 `npx init` 是否有用」的疑问：`init` 对已有 CLAUDE.md 非破坏（追加或就地刷新 marker block，不覆盖原有内容），价值在于接入 `.project/ai-rules.md` 规则链 + 生成项目管理脚手架；已初始化过的仓库应改用 `update`。
- 回答用户关于 RESEARCH 更新触发条件的疑问：此前 RESEARCH 没有任何触发条件，只有占位 README，属于规则缺口。
- 应用户要求补齐 RESEARCH 更新触发条件：当产生新的技术发现、外部参考资料或调研结论时写入 `docs/RESEARCH/`。

## Decisions

- 为 RESEARCH 增加明确更新触发条件（技术发现/外部参考/调研结论），并同步 ai-rules/workflow/prompts 及 `bin/ai-project-os.js` 内置模板；VERSION 与 package.json bump 到 0.5.0。详见 [DECISIONS.md](../DECISIONS.md)。

## Action Items

- 将 `ai-project-os@0.5.0` 发布到 npm 并端到端验证 RESEARCH 规则已注入（未执行，待用户确认是否发布）。

## Notes

- 实文件与内置模板字符串已对齐；`node --check` 通过；`update --dry-run` 正常。
- 本仓库 `.project/config.yaml` 已手动同步到 0.5.0（未跑 `update` 以避免产生 `.project-os-backups/` 备份目录）。
- 文档此前漏更新——用户追问「为啥没有doc更新」后才补齐 STATUS/NEXT_ACTION/DECISIONS/本会议记录；教训是改变项目决策/行为后应立即同步项目状态文档。
