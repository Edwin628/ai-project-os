# 2026-07-05

## Summary

- 讨论了 AI Project OS 在已有 Git 项目中的初始化方式。
- 确定不要复制整个仓库，而是提供一键初始化器，将 `.project/`、`docs/`、`prompts/` 注入目标项目。
- 实现了 Node CLI 和 `install.sh` 两条安装路径。

## Decisions

- 默认安装路径使用版本 tag，而不是可变的 `main`。
- 项目文档语言继续由 `.project/config.yaml` 的 `project_os.doc_language` 控制，默认 `same-as-user`。

## Action Items

- 检查本次文件变更并提交。
- 创建并推送 `v0.1.0` Git tag。
- 在干净目录中验证公开安装命令。

## Notes

- 安装器默认不覆盖已有文件，除非用户显式传入 `--force`。
- `--dry-run` 可用于预览将创建、覆盖或跳过的文件。
- 远程安装属于供应链敏感路径，后续发布应继续推进固定版本、校验和或 npm 正式发布。
