# 2026-07-11

## Summary

- 将 ai-project-os@0.3.0 发布到 npm 官方 registry，并端到端验证部署功能。
- 补充 package.json 发布字段与 README 的 npm 安装说明，提交并推送（commit `944a028`）。

## Decisions

- 发布到 npm 官方 registry，使用 granular access token（bypass 2FA）发布，发完吊销。
- npm 包 files 白名单只含 bin/install.sh/README，不含项目状态文档，避免泄露本仓库内部状态。

## Action Items

- [ ] 在 npmjs.com 吊销曾暴露的 granular token（`npm_a161…`）。
- [ ] 规划下一个版本（如 v0.4.0）的发布流程。

## Notes

- npm 全局 registry 是淘宝镜像（只读），发布必须 `--registry https://registry.npmjs.org`。
- 账号开 2FA 但无 TOTP app，发布走 granular token 而非 OTP。
- 发现 npm v10 空目录裸 install 边缘行为；发现 `npx <pkg> --version` 会被 npx 吞掉 flag。
- 反思：今天会话未触发 ai-project-os 文档更新，根因是仓库缺 CLAUDE.md/hook，规则未接入 AI 工作流；已记入 BACKLOG。

## v0.4.0 --agents 实现

- 实现 `--agents` AI 工具 shim（A 方案：init 内置默认 claude+codex，marker block 块级管理，薄壳指针不复制规则）。
- AGENTS 注册表：claude/codex/cursor（带 frontmatter）/copilot/gemini/cline/windsurf。
- 本地 dry-run 全场景测试通过：init/update/uninstall、all/none、已有文件追加块、块移除保留用户内容、仅块文件 uninstall 后删除、cursor frontmatter。
- 待：打 v0.4.0 git tag + npm publish + npx 端到端验证。

## 接规则到 Claude Code 工作流

- 根因复盘：本仓库缺 `CLAUDE.md` 和 hook，`.project/ai-rules.md` 规则不进 AI 上下文，导致会话中漏更新 NEXT_ACTION（规则"知道"≠"在节点执行"）。
- 用 v0.4.0 自己的 `update --agents claude` dogfood：生成 `CLAUDE.md`（marker block 指向 ai-rules.md）+ 升级 config 到 0.4.0 + 加 `agents: [claude]`；顺带在真实项目验证 update 路径。
- 配 `.claude/settings.json` SessionStart hook：`printf` 一句提醒读 `.project/ai-rules.md`；CLAUDE.md 是正式入口，hook 只做提醒不注入全文。
- 加 `.gitignore` 排除 `.project-os-backups/` 与 `.claude/settings.local.json`。
- 待：新会话验证 hook 是否触发。
